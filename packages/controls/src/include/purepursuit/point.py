class Point:
    """
    Represents a point in (x, y, theta)
    """
    def __init__(self, 
                 x: float, 
                 y: float, 
                 heading: float):
        self.x = x 
        self.y = y 
        self.heading = heading
